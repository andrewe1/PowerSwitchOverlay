<Window x:Class="PowerSwitchOverlay.SplashWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Loading"
        Width="180" Height="40"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize">
    
    <!-- WHY: Styled splash matching app theme - dark background, teal accent, lightning icon -->
    <Border Background="#1E1E1E" CornerRadius="8" BorderBrush="#00D4AA" BorderThickness="1">
        <Grid Margin="6">
            <!-- WHY: Single centered lightning bolt for minimal splash -->
            <TextBlock Text="⚡" FontSize="18" Foreground="#00D4AA" 
                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Grid>
    </Border>
</Window>
